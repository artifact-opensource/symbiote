node_modules/
dist/
.sessions/
*.js
*.js.map
*.d.ts
!src/**

# Secrets & local config — never commit
.env
symbiote.json

# Keep templates — these ARE committed
!.env.example

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# WhatsApp auth (contains session keys)
whatsapp-auth/
*.tgz

# Local runtime directories — not part of source
.ava/
comb/
hektor/
symbiote-core/
symbiote.json.bak

# Agent identity files — per-instance, never commit
IDENTITY.md
SOUL.md
USER.md
AGENTS.md
HEARTBEAT.md

# Runtime logs
*.log

# Instance-specific launch scripts
start.vbs
scarlet-start.bat

# COMB local data
.comb/

# Archive/staging (local session data)
archive/
staging/
-e 
# Webapp production build — committed for deployment
!webapp/dist/

# Runtime data and memory
.ava-memory/
.vdb/
.mcp/
.pulse/
.vendored-libs/
mach6-core/

# Backups and old versions
*.bak
webapp_old/
webapp_old_backup_*/
.archived-webapp/
.env.archive
